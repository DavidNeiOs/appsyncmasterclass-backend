service: appsyncmasterclass-backend
frameworkVersion: "2"

plugins:
  - serverless-appsync-plugin

provider:
  name: aws
  runtime: nodejs12.x

functions:
  hello:
    handler: handler.hello# you can add CloudFormation resource templates here

package:
  exclude:
    - yarn.lock
    - package.json

custom:
  appsync:
    - ${file(serverless-appsync-api.yml)}
# resources:
#  Resources:
#    NewResource:
#      Type: AWS::S3::Bucket
#      Properties:
#        BucketName: my-new-bucket
#  Outputs:
#     NewOutput:
#       Description: "Description for the output"
#       Value: "Some output value"
